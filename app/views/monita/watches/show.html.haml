%h1 Watch: #{watch.name}

%h3
  Status:
  - if watch.last_updated_at < 2.minutes.ago
    CRITICAL
  - elsif watch.last_updated_at < 1.minute.ago
    WARNING
  - else
    OK

%p last update: #{l watch.last_updated_at}